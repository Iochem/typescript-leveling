# Mini Project - Cache System

## 🚀Features
- Add items to cache using a key-value structure
- Retrieve cached values by key
- In-memory cache storage
- Methods (add / get)
- Encapsulated cache access (no direct storage manipulation)

## 📚 Concepts
- *Classes* and *constructors*
- *Optional return values* (`string | undefined`)
- *Access modifiers* (`private`, `protected`, `readonly`)
- *Oop* (encapsulation, composition, practical abstraction, SRP)
- Uses a typed array (`CacheItem[]`) as an internal data structure

## 🛠️Technologies]
- TypeScript
- Node.js
- ts-node